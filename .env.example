# Django
SECRET_KEY=change-me-to-a-real-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite is used by default if DATABASE_URL is not set)
# DATABASE_URL=postgres://user:password@localhost:5432/matrimony

# Email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
